from typing import List


class Solution:
    def minSubArrayLen(self, target: int, nums: List[int]) -> int:
        left = 0
        smallest = 0
        current = 0
        len = len(nums)

        for right in range(0, len):
            current += nums[right]
            if 
            if right - left:



